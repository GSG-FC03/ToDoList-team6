// declareing the variabale 
const inpText = document.getElementById('input-text');
const btnAdd = document.getElementById('key-add');
const keyinp = document.getElementById('key-inp');
const result = document.querySelector(".result");
const removeBtn = document.getElementById('delete-btn');
const editContent = document.getElementById('edit-btn');

// declareing the variabale 